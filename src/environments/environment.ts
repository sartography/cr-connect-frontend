import {AppEnvironment} from '../app/_models/app-environment';

export const environment: AppEnvironment = {
  production: false,
  api: 'localhost:4200',
  dummy: true,
  // api: 'localhost:5000',
};
